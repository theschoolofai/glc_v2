teams: rejected inbound Activity JWT: Invalid header string: 'utf-8' codec can't decode byte 0x81 in position 0: invalid start byte
[1] real owner paired once, as the legitimate flow would leave it: '29:teams-real-owner'
[2] a *real* (no mock configured) Adapter instance is fed a forged Activity with
    zero proof of Bot Framework origin (no Authorization header, nothing):
    on_message(...) -> None (rejected)

[3] production-shaped request, no Authorization header:
    on_message({'raw_body': ..., 'headers': {}}) -> None
[4] production-shaped request, forged/garbage bearer token:
    on_message(..., headers={'authorization': 'Bearer garbage...'}) -> None
[5] production-shaped request, correctly-signed JWT (verified against a test JWKS):
    on_message(..., headers=<valid JWT>) -> trust_level='owner_paired'

RESULT: NOT VULNERABLE - the direct-forgery path is closed, requests with no/invalid Authorization are rejected, and a correctly-signed JWT is accepted and attributed correctly.
exit code: 0
